// /** @type {import('next').NextConfig} */
// const nextConfig = {};
//
// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['snzdsxhpqwfpdluumukd.supabase.co'], // הוסף את הדומיין של Supabase כאן
    },
};

export default nextConfig;

